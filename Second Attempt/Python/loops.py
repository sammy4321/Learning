fruits = ['apple', 'banana', 'cherry']

for fruit in fruits:
    print(fruit)
else:
    print("Loop completed successfully.") # Else is run after the loops is done. If you use break to stop the loop, then else is skipped.

count = 0

while count < 5:
    print(count)
    count += 1
else:
    print("Counting finished.") # Else is run after the loops is done. If you use break to stop the loop, then else is skipped.
